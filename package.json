{
  "version": "1.0.0",
  "private":true,
  "scripts": {
    "designer": "pnpm -F report-designer dev",
    "postbuild": "node scripts/postBuild.js",
    "build": "pnpm -F report-designer build && pnpm -F report-excel build && pnpm -F report-plugin-jasperreport build",
    "preinstall": "npx only-allow pnpm"
  },
  "devDependencies": {
    "@types/react": "^18.0.37",
    "@types/react-dom": "^18.0.11",
    "@vitejs/plugin-react": "^4.0.0",
    "prettier": "^3.1.0",
    "vite": "^4.3.9"
  }
}