import { createSlice } from '@reduxjs/toolkit';

export const conditionStyleSlice = createSlice({
    name: 'layoutSlice',
    initialState: {},
    reducers: {},
});
export const {} = conditionStyleSlice.actions;
export default conditionStyleSlice.reducer;
